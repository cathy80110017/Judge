#include <stdio.h>
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
int main(){
    vector< vector<int>> a;
    char comma;
    int i = 0;
    while (cin >> a[i][0] >> comma >> a[i][1]){
        if (cin >>  >>){
            
        }
        
    }
    return 0;
}